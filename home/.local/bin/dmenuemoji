#!/bin/sh

chosen=$(cut -d ';' -f1 ~/.emojis | dmenu -i -l 30 | sed "s/ .*//")

[ -z "$chosen" ] && exit

printf "%s" "$chosen" | xclip -selection clipboard
notify-send -i ' ' "$chosen copied to clipboard" &
